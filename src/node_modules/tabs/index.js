import { prettifyCamelCase } from 'utils'
import autumnLeaves from './images/autumnLeaves.png';

const tabs = {autumnLeaves};

export default Object.entries(tabs).map(([key, value]) => ({title: prettifyCamelCase(key), slug: key, image: value}));